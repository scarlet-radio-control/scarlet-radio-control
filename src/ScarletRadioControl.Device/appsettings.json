{
	"$schema": "https://www.schemastore.org/appsettings.json",
	"ConnectionStrings": {
		"HubConnection": "https://scarlet-radio-control-web.azurewebsites.net/hubs/web-rtc-hub"
	},
	"Device": {
		"DeviceId": "5454d100-26bf-4c9b-bec7-0289aad847d4",
		"Camera": {
			"LinuxPath": "/dev/video0",
			"WindowsPath": "video=Integrated Camera",
			"Width": 1280,
			"Height": 720,
			"Framerate": 30
		},
		"Ffmpeg": {
			"BitrateKilobitsPerSecond": 1500,
			"ExecutablePath": "ffmpeg",
			"KeyframeIntervalSeconds": 2,
			"LinuxArguments": "-hide_banner -nostats -loglevel warning -fflags nobuffer -flags low_delay -f v4l2 -input_format mjpeg -video_size {Width}x{Height} -framerate {Framerate} -thread_queue_size 512 -i {CameraPath} -an -c:v libx264 -pix_fmt yuv420p -profile:v baseline -preset superfast -tune zerolatency -sc_threshold 0 -bf 0 -x264-params repeat-headers=1 -b:v {Bitrate} -maxrate {Bitrate} -bufsize {BufferSize} -g {GopFrames} -keyint_min {GopFrames} -f rtp -payload_type {PayloadType} rtp://localhost:{RtpPort}?pkt_size=1200",
			"RtpPort": 0,
			"WindowsArguments": "-hide_banner -nostats -loglevel warning -fflags nobuffer -flags low_delay -f dshow -rtbufsize 64M -video_size {Width}x{Height} -framerate {Framerate} -vcodec mjpeg -i {CameraPath} -an -c:v libx264 -pix_fmt yuv420p -profile:v baseline -preset superfast -tune zerolatency -sc_threshold 0 -bf 0 -x264-params repeat-headers=1 -b:v {Bitrate} -maxrate {Bitrate} -bufsize {BufferSize} -g {GopFrames} -keyint_min {GopFrames} -f rtp -payload_type {PayloadType} rtp://localhost:{RtpPort}?pkt_size=1200"
		}
	},
	"Logging": {
		"LogLevel": {
			"Default": "Information"
		}
	}
}